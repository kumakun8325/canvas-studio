すべてのテストがパスしました（34 passed, 1 skipped）。

## まとめ

Issue #35 の修正が完了しました。

### 修正内容
`src/hooks/useCanvas.ts` において、`currentSlideId` のクロージャキャプチャ問題を修正しました。

**変更点:**
1. `currentSlideIdRef` を追加 (`useRef<string | null>(null)`)
2. `useEffect` で `currentSlideId` が変更されるたびに `currentSlideIdRef.current` を更新
3. `handleSave` 内で `currentSlideId` の代わりに `currentSlideIdRef.current` を使用

### TDD プロセス
- **RED**: `task35-undo-bug.test.tsx` でバグを再現（最初のテストケースが失敗）
- **GREEN**: `useRef` を使用した修正でテストがパス
- **REFACTOR/REVIEW**: コードレビュー完了、最小限の変更で問題を解決
