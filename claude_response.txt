⚠️  [BashTool] Pre-flight check is taking longer than expected. Run with ANTHROPIC_LOG=debug to check for failed or slow API requests.
⚠️  [BashTool] Pre-flight check is taking longer than expected. Run with ANTHROPIC_LOG=debug to check for failed or slow API requests.
## まとめ

### Issue #75: ツールバーの↑↑↓↓ボタンの機能が不明瞭・UIの改善が必要

**完了した対応:**

1. **機能の特定**
   - ↑↑ ボタン: `bringToFront` - 選択したオブジェクトを最前面に移動
   - ↓↓ ボタン: `sendToBack` - 選択したオブジェクトを最背面に移動

2. **UI/UX改善**
   - ツールチップ文言をより明確に変更:
     - `title="前面に移動"` → `title="最前面に移動"`
     - `title="背面に移動"` → `title="最背面に移動"`
   - アイコンを変更して直感性向上:
     - `↑↑` → `⬆️` （上矢印絵文字）
     - `↓↓` → `⬇️` （下矢印絵文字）
   - `text-lg` クラス追加で視認性向上

3. **アクセシビリティ向上**
   - `aria-label` 属性を追加:
     - 「選択したオブジェクトを最前面に移動」
     - 「選択したオブジェクトを最背面に移動」

4. **テスト追加**
   - 5つのテストケースを追加:
     - ボタンのレンダリングとツールチップ確認
     - クリック時の動作確認
     - aria-label の確認

**変更ファイル:**
- `src/components/canvas/Toolbar.tsx` (変更)
- `src/components/canvas/Toolbar.test.tsx` (新規追加)

**検証結果:**
- ✅ 全テストパス (182 passed)
- ✅ ビルド成功
- ✅ ESLintチェック通過（変更ファイル）
- ✅ 既存機能に影響なし
